[project]
name = "anna-library"
version = "0.1.0"
description = "CLI tool to search and download books from Anna's Archive"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.31",
    "beautifulsoup4>=4.12",
    "python-dotenv>=1.0",
]

[project.scripts]
anna = "anna:main"
